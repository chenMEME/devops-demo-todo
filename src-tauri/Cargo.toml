[package]
name = "todo-desktop"
version = "1.0.0"
description = "📋 待办事项 — Windows 桌面应用"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
